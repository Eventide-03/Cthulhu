# Pixel-font slot

Drop the pixel font here as `pixel.woff2`, then uncomment its line in
`theme/jar.mn`. It resolves at `chrome://cthulhu/content/assets/fonts/pixel.woff2`
and binds the `Cthulhu Pixel` family (see the `@font-face` in `theme/content/theme.css`,
consumed via `var(--font-pixel)`).
